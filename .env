# This is the .env file for environment variables.
# Example:
# DATABASE_URL=postgresql://user:password@localhost:5432/servicedb
# SECRET_KEY=your_secret_key

# For docker-compose setup, DATABASE_URL is often set within docker-compose.yml
# This file can be used for local development outside Docker or for other environment-specific settings.
DATABASE_URL=postgresql://user:password@db:5432/servicedb
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=servicedb
